#include <iostream>
using namespace std;

// Main function is the entry point of the program
int main()
{
    cout << "Hello World!"; // std is the namespace, cout is the object of ostream class and << is the insertion operator which is used to insert the data into the object
    return 0;
}
